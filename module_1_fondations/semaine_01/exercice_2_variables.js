// ╔═══════════════════════════════════════════════════════════════════╗
// ║              EXERCICE 2 : Créer des variables                     ║
// ╚═══════════════════════════════════════════════════════════════════╝
//
// Une variable, c'est une boîte pour stocker une valeur.
// C'est comme les variables orange dans Scratch !
//
// Pour créer une variable :
//   let nomDeLaVariable = valeur;
//
// Exemples :
//   let score = 0;
//   let pseudo = "Alex";
//   let vies = 3;
//
// ═══════════════════════════════════════════════════════════════════

// 2.1 Crée une variable "pseudo" avec ton prénom et affiche-la
let pseudo;  // Remplace par : let pseudo = "TonPrenom";
console.log(pseudo);

// 2.2 Crée une variable "anneeNaissance" avec ton année de naissance
let anneeNaissance;  // Remplace par la bonne valeur
console.log(anneeNaissance);

// 2.3 Crée une variable "jeuPrefere" avec le nom de ton jeu préféré
let jeuPrefere;  // À toi de jouer !
console.log(jeuPrefere);

// 2.4 Crée une constante "ANNEE_ACTUELLE" avec la valeur 2025
// Une constante se crée avec "const" au lieu de "let"
// Elle ne peut pas être modifiée après
const ANNEE_ACTUELLE;  // Remplace par : const ANNEE_ACTUELLE = 2025;
console.log(ANNEE_ACTUELLE);

// 2.5 Calcule et affiche ton âge (utilise les variables créées)
let age;  // Indice : age = ANNEE_ACTUELLE - anneeNaissance
console.log("Mon âge :", age);
